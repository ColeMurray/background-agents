feat(linear): add Linear integration for task and issue linking

Control plane:
- Linear GraphQL client (listIssues, listTeams, createIssue, updateIssue, getIssue)
- Routes: GET /linear/issues, GET /linear/teams, POST /sessions/:id/linear/link-task,
  POST /sessions/:id/linear/create-issue, PATCH /linear/issues/:id
- Schema: session.linear_issue_id, linear_team_id; task_linear_links table
- DO: internal/linear/link-task; state includes linearIssueId, linearTeamId, taskLinearLinks
- Broadcast/replay events include id and messageId for stable task keys
- Spawn sandbox: optional linear context in Modal config when session has linked issue

Modal:
- SessionConfig.linear (LinearContext); web_api accepts linear in create-sandbox payload

Web:
- Task type with messageId, eventId, taskIndex; TaskLinearLink, LinearIssue types
- extractLatestTasks returns task keys for linking
- API routes proxying to control plane for Linear; lib/linear client helpers
- Tasks section: Linear badge, Link to Linear (picker), Create issue (team required)
- Metadata: Linked to Linear when session has linearIssueId
- Session state and sidebar pass sessionId, taskLinearLinks

Terraform:
- Optional linear_api_key (LINEAR_API_KEY) for control plane worker

Docs:
- LINEAR_INTEGRATION_PLAN with Phase 1 implementation notes

Also: remove design-lab page
