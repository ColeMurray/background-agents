# Background Agents - Environment Configuration
# Copy this to .env and fill in your API keys.

# ─── LLM API Keys (at least one required) ────────────────────────────────────
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
# OPENROUTER_API_KEY=

# ─── Server Configuration ────────────────────────────────────────────────────
# PORT=8787
# HOST=0.0.0.0

# ─── Repos Directory ─────────────────────────────────────────────────────────
# Path to a directory containing git repositories to work on.
# Default: ~/code
# REPOS_DIR=/Users/you/code

# ─── Data Directory ──────────────────────────────────────────────────────────
# Where session data (SQLite DB) is stored.
# Default: ~/.local/share/background-agents
# DATA_DIR=

# ─── Docker ──────────────────────────────────────────────────────────────────
# Sandbox Docker image name. Built from packages/sandbox/Dockerfile.
# SANDBOX_IMAGE=background-agents-sandbox
