# Background Agents - Environment Configuration
# Copy this to .env and customize as needed.

# ─── LLM Authentication ──────────────────────────────────────────────────────
#
# Option 1 (recommended): Use your existing OpenCode login.
#   If you've already run `opencode` and logged in with your Anthropic Pro/Max
#   or ChatGPT Plus/Pro account, auth carries over automatically — the sandbox
#   mounts ~/.local/share/opencode/auth.json into each container.
#   No env vars needed.
#
# Option 2: Explicit API keys.
#   Set these if you prefer to use API keys instead of OAuth login.
#
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
# OPENROUTER_API_KEY=

# ─── Server Configuration ────────────────────────────────────────────────────
# PORT=8787
# HOST=0.0.0.0

# ─── Repos Directory ─────────────────────────────────────────────────────────
# Path to a directory containing git repositories to work on.
# Default: ~/code
# REPOS_DIR=/Users/you/code

# ─── Data Directory ──────────────────────────────────────────────────────────
# Where session data (SQLite DB) is stored.
# Default: ~/.local/share/background-agents
# DATA_DIR=

# ─── Docker ──────────────────────────────────────────────────────────────────
# Sandbox Docker image name. Built from packages/sandbox/Dockerfile.
# SANDBOX_IMAGE=background-agents-sandbox
